# Baseline agent catalog fixture for future harness and role tests.
default_role: builder
harness_configs:
  pi:
    harness: pi
    command:
      - pi
      - --model
      - "{model}"
      - -p
      - "{prompt}"
roles:
  builder:
    harness_config: pi
    model: lmstudio/qwen3.6-35b-a3b-uncensored-heretic-native-mtp-preserved
    dispatch_hint: Implement the assigned task only. Stay in scope. Return files changed, checks run, results, blockers, and risks.
