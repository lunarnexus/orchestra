[build-system]
requires = ["setuptools>=69", "wheel", "setuptools-scm>=8"]
build-backend = "setuptools.build_meta"

[project]
name = "orchestra"
dynamic = ["version"]
description = "Agent-agnostic orchestration control plane for focused worker agents"
readme = "README.md"
requires-python = ">=3.11"
license = "MIT"
authors = [{ name = "LunarNexus" }]
dependencies = [
  "PyYAML>=6.0",
]
classifiers = [
  "Programming Language :: Python :: 3",
  "Programming Language :: Python :: 3.11",
  "Operating System :: OS Independent",
]

[project.scripts]
orchestra = "orchestra.cli:main"

[project.optional-dependencies]
dev = [
  "build>=1.2.1",
  "mypy>=1.10",
  "pytest>=8.2",
  "ruff>=0.5",
  "types-PyYAML>=6.0",
]

[tool.setuptools]
package-dir = {"" = "src"}
include-package-data = true

[tool.setuptools.packages.find]
where = ["src"]

[tool.setuptools_scm]
version_file = "src/orchestra/_version.py"
tag_regex = "^v(?P<version>\\d+\\.\\d+\\.\\d+)$"

[tool.setuptools.package-data]
orchestra = [
  "assets/pi/orchestra/index.ts",
  "assets/opencode/orchestra/index.ts",
  "assets/opencode/orchestra/commands/orch.md",
  "assets/codex/orchestra/.codex-plugin/plugin.json",
  "assets/codex/orchestra/skills/orchestra/SKILL.md",
]

[tool.setuptools.data-files]
"share/orchestra" = [
  "config.yaml",
  "prompts.yaml",
  "agent-catalog.yaml",
]

[tool.pytest.ini_options]
addopts = "-ra --ignore=evals --ignore=docs/research"

[tool.ruff]
target-version = "py311"
line-length = 100
extend-exclude = ["evals", "docs/research"]

[tool.ruff.lint]
select = ["B", "E", "F", "I", "UP"]

[tool.mypy]
python_version = "3.11"
warn_unused_configs = true
pretty = true
show_error_codes = true
strict = true
mypy_path = "src"
